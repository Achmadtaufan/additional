SELECT  
    j.name AS job_name,
    h.run_date,
    h.run_time,
    h.run_status,
    h.message
FROM msdb.dbo.sysjobs j
JOIN msdb.dbo.sysjobhistory h 
    ON j.job_id = h.job_id
WHERE h.message LIKE '%sp_getdata%'
ORDER BY h.run_date DESC, h.run_time DESC;