SELECT 
    kolom_tanggal,
    CASE 
        WHEN kolom_tanggal LIKE '00/00/%' 
            THEN RIGHT(kolom_tanggal, 4) + '0000'
        ELSE CONVERT(VARCHAR(8), TRY_CONVERT(DATE, kolom_tanggal, 103), 112)
    END AS tgl_format
FROM nama_tabel;