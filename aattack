SELECT cp.clientnum
FROM clientproduct cp
GROUP BY cp.clientnum
HAVING SUM(
    CASE WHEN cp.product IN (
        SELECT product 
        FROM product 
        WHERE tabledesc = 'enroltpa'
    ) THEN 1 ELSE 0 END
) = 0;