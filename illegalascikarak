SELECT filename
FROM excelupload
WHERE EXISTS (
    SELECT 1
    FROM master..spt_values v
    WHERE v.type = 'P'
      AND v.number BETWEEN 1 AND LEN(filename)
      AND UNICODE(SUBSTRING(filename, v.number, 1)) > 127
);